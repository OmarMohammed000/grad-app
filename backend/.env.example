# Copy this file to .env and fill in your actual values

# Environment Configuration
NODE_ENV=development

# Database Configuration (Single DB for all environments)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grad_app_db
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# Server Configuration
# ⚠️ IMPORTANT: Default port is 4000 (make sure frontend uses http://localhost:4000)
PORT=4000

# JWT Authentication
JWT_SECRET=your_jwt_secret_here_min_32_chars
JWT_REFRESH_SECRET=your_jwt_refresh_secret_here_min_32_chars

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
# ⚠️ CRITICAL: Use the SAME Client ID in both frontend and backend!
# 
# How to get these:
# 1. Go to Google Cloud Console
# 2. APIs & Services → Credentials
# 3. Create OAuth 2.0 Client ID (Web Application)
# 4. Copy both Client ID and Client Secret
# 5. In frontend/.env, use the SAME Client ID for EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET

# Bcrypt Configuration
BCRYPT_SALT_ROUNDS=10